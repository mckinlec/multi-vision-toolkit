# Core PyTorch packages - pinned for compatibility
torch==2.6.0
torchvision==0.21.0
# torchaudio==2.6.0  # Only include if actually needed

# Transformers - use git version that supports Qwen2.5-VL
git+https://github.com/huggingface/transformers.git  # Latest with Qwen2.5-VL support
# Alternative if git version doesn't work:
# transformers==4.46.3

# Core dependencies
Pillow>=10.0.0
timm==0.9.12
einops>=0.7.0
python-dotenv>=1.0.0
packaging>=21.0
opencv-python>=4.8.0
matplotlib>=3.5.0
accelerate>=0.26.0
safetensors>=0.4.0
tensorboard>=2.15.0
numpy>=1.24.0,<2.0.0  # Prevent numpy 2.0 issues
tqdm>=4.66.0
tkinterdnd2>=0.3.0
bitsandbytes>=0.42.0
requests>=2.31.0  # For custom API models

# Optional: Flash Attention (install separately if needed)
flash-attn>=2.5.0  # pip install flash-attn --no-build-isolation